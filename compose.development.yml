name: raven-url-shortener

services:
    # https://hub.docker.com/_/redis
    redis:
        image: redis:alpine
        command: >
            --requirepass ${REDIS_PASSWORD} --maxmemory 128mb --maxmemory-policy allkeys-lru
        volumes:
            - ./server/docker/services/redis:/usr/local/etc/redis
        healthcheck:
            test: redis-cli --raw incr ping
            retries: 3
            timeout: 5s
        ports:
            - "6379:${REDIS_PORT}"

    # https://hub.docker.com/_/postgres
    postgres:
        image: postgres:alpine
        volumes:
            - ./server/docker/services/postgres:/var/lib/postgresql/data
        environment:
            POSTGRES_DB: ${DATABASE_NAME}
            POSTGRES_USER: ${DATABASE_USERNAME}
            POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
        healthcheck:
            test: pg_isready --dbname=${DATABASE_NAME} --username=${DATABASE_USERNAME}
            retries: 3
            timeout: 5s
        ports:
            - "5432:${DATABASE_PORT}"

    # https://hub.docker.com/r/dpage/pgadmin4/
    pgadmin:
        image: dpage/pgadmin4:latest
        volumes:
            - ./server/docker/services/pgadmin:/var/lib/pgadmin
        depends_on:
            - postgres
        environment:
            PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
            PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD}
        healthcheck:
            test: wget -O - http://localhost
            retries: 3
            timeout: 5s
        ports:
            - "8080:80"

    # https://github.com/FlorianLeChat/Raven-Url-Shortener
    php:
        image: raven-url-shortener-backend
        volumes:
            - ./server:/var/www/html
        command: ./docker/scripts/development/entrypoint.sh
        depends_on:
            - redis
            - postgres
        environment:
            - WAIT_HOSTS=redis:${REDIS_PORT}, postgres:${DATABASE_PORT}
        healthcheck:
            test: curl -f http://127.0.0.1:9000/ 2>&1 | grep "Connection reset by peer"
            retries: 3
            timeout: 5s
        build:
            context: ./server
            dockerfile: ./docker/scripts/development/Dockerfile
        ports:
            - "8000:8000"